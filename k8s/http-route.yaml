apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: personal-blog-route
  namespace: personal-blog
spec:
  parentRefs:
    - name: web-gateway
      namespace: traefik
  hostnames:
    - k8s.gabrielcarvalho.dev
  rules:
    - backendRefs:
        - name: personal-blog-svc
          port: 80
